{
  "compilerOptions": {
    "jsx": "react-jsx", // or "react" for older versions of React
    "strict": true,      // Optional, for stricter type checking
    "moduleResolution": "node",  // Recommended for React projects
    "esModuleInterop": true        // Optional, for compatibility with CommonJS modules
  }
}